name: Greetings

on: [pull_request, issues]

jobs:
  greeting: vittu lähen röökille
    runs-on:  ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}github
        issue-me 
